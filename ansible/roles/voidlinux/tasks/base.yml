- name: base > Install base tools
  xbps: name={{ item }}
  with_items:
    - acpi
    - avahi, avahi-utils
    - curl
    - exfat-utils
    - fish-shell
    - gcc
    - git
    - make
    - nmap
    - lm_sensors
    - lshw
    - rcm
    - rsync
    - screen
    - smbclient
    - tree
    - unzip
    - unrar
    - wget
    - xtools
    - xz

- name: base > Enable base services
  runit: name={{ item }} enabled=yes state=started
  with_items:
    - avahi-daemon
